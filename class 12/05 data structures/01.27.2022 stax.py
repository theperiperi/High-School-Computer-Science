def push(name):
    s.append(names)
    print(names)

def pop(names):
    if len(s)==0:
        print ('stack is empty')
    else:
        names=s.pop()
        print('value deleted from stack')

push(package) and pop(package)
to add a new package from a list of packages, considering them to act as
push and pop operations of the stack data structure.

